Brad = "Brad"

def bradsFunction():
    bradsName = "Brad"
    print(Brad)
    return bradsName

class userInput:
        def __init__(self, firstName, lastName):
            self.firstName = firstName
            self.lastName = lastName
            self.addresses = []
        
        def addresses(self):
            self.addresses.append()
            return

class userAddress(userInput):
        def __init__(self, street, city, state, zipCode):
            super().__init__(street, city, state, zipCode)
            self.street = street
            self.city = city
            self.state = state
            self.zip_code = zipCode

    

Ashton = userAddress("Ashton","Bentley", "23 s merry mount", "WA")
print(Brad)
    